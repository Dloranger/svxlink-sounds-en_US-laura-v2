At low signal strengths, use a squelch hang time so that it is less likely the squelch will close unexpectedly. This variable is unset by default. A value of 1000 milliseconds may be a good value to start out with. 

To use the extended squelch hang time feature, set up the SQUELCH EXTENDED HANG TIME THRESH variable as well.